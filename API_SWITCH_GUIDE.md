# API Переключение: Тестовый ↔ Реальный

## 🔄 ТЕКУЩАЯ КОНФИГУРАЦИЯ

**Активно:** Тестовый API (заглушка)
- Все запросы идут к `https://domain-inspector-backend.vercel.app/api/inspect?domain=google.com`
- Ответ модифицируется - домен подменяется на запрошенный
- Позволяет тестировать интерфейс с реальными данными

---

## 🚀 ПЕРЕКЛЮЧЕНИЕ НА РЕАЛЬНЫЙ API

### Шаг 1: Файл `src/lib/api.ts`

**ЗАКОММЕНТИРОВАТЬ (тестовый режим):**
```typescript
// === ТЕСТОВЫЙ API (ЗАГЛУШКА) ===
// Для тестирования используем всегда google.com как рабочий домен
// TODO: Убрать эту заглушку при подключении реального API
const testDomain = 'google.com';
const url = `https://domain-inspector-backend.vercel.app/api/inspect?domain=${encodeURIComponent(testDomain)}`;

// === ТЕСТОВЫЙ API (МОДИФИКАЦИЯ ОТВЕТА) ===
// Подменяем домен в ответе на запрошенный для тестирования
// TODO: Убрать эту модификацию при реальном API
if (data.data && data.data.domain) {
  data.data.domain = domain;
}
```

**РАСКОММЕНТИРОВАТЬ (реальный режим):**
```typescript
// === РЕАЛЬНЫЙ API (ЗАКОММЕНТИРОВАНО) ===
// TODO: Раскомментировать для реального API
const url = `${config.apiBaseUrl}/api/inspect?domain=${encodeURIComponent(domain)}`;
```

### Шаг 2: Файл `src/lib/config.ts`

**Проверить базовый URL:**
```typescript
// API
apiBaseUrl: import.meta.env.VITE_API_BASE_URL || 'https://your-real-api-domain.com',
```

### Шаг 3: Environment Variables

**Создать/обновить `.env`:**
```bash
VITE_API_BASE_URL=https://your-real-api-domain.com
VITE_API_TIMEOUT=10000
```

---

## 🧪 ТЕСТИРОВАНИЕ РЕАЛЬНОГО API

### 1. Проверка доступности
```bash
curl "https://your-api-domain.com/api/inspect?domain=google.com"
```

### 2. Проверка формата ответа
Ожидаемый формат:
```json
{
  "success": true,
  "data": {
    "domain": "google.com",
    "created": "1997-09-15T04:00:00Z",
    "expires": "2028-09-14T04:00:00Z",
    "registrar": "MarkMonitor Inc.",
    "ip": "142.250.185.78",
    "country": "US",
    "asn": "AS15169 Google LLC",
    "ns": ["ns1.google.com", "ns2.google.com", "ns3.google.com", "ns4.google.com"]
  },
  "cached": false
}
```

### 3. Обработка ошибок
API должен возвращать корректные HTTP статусы:
- `404` - домен не найден
- `429` - превышен лимит запросов
- `500` - внутренняя ошибка сервера

---

## 🔬 ОТЛАДКА API

### Console команды для тестирования:

**В popup console:**
```javascript
// Тестовый запрос
import { inspectDomain } from '@/lib/api';
inspectDomain('example.com').then(console.log);

// Прямой fetch
fetch('https://your-api-domain.com/api/inspect?domain=google.com')
  .then(r => r.json())
  .then(console.log);
```

**В background console:**
```javascript
// Проверка конфигурации
import { config } from '@/lib/config';
console.log('API Base URL:', config.apiBaseUrl);
```

---

## 📋 CHECKLIST ПЕРЕКЛЮЧЕНИЯ

- [ ] Закомментирован тестовый код в `api.ts`
- [ ] Раскомментирован реальный URL в `api.ts`
- [ ] Убрана модификация ответа (подмена домена)
- [ ] Обновлен `apiBaseUrl` в `config.ts`
- [ ] Созданы/обновлены environment variables
- [ ] Проверена доступность реального API
- [ ] Протестирован формат ответа
- [ ] Пересобран проект (`npm run build`)
- [ ] Протестировано расширение с реальным API

---

## 🚨 ВОЗВРАТ К ТЕСТОВОМУ API

Если реальный API недоступен:

1. Вернуть комментарии в `src/lib/api.ts`
2. Снова использовать тестовый заглушку
3. Пересобрать проект

---

## 📝 ЗАМЕЧАНИЯ

- **Тестовый режим** всегда возвращает данные google.com
- **Реальный режим** запрашивает данные для каждого домена
- При переключении **обязательно пересобрать** проект
- Проверять **CORS** настройки реального API
- Мониторить **лимиты** реального API (rate limiting)
